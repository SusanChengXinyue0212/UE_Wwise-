# Unreal & Wwise 音频设计实践——开发说明文档

**24122347 程心玥**

---

## 一、概述

本次项目基于 Unreal Engine 和 Wwise 音频中间件，完成了空间音频（Room and Portal）、基础环境声、物件 3D Object 和角色脚步（Character Foley Footstep）四个模块的音频设计实践。以下主要记录开发过程中遇到的上手困难与解决思路。

---

## 二、开发过程与想法

本次实践以熟悉 UE 与 Wwise 的协同工作流程为主要目标。由于此前对两款工具均缺乏实操经验，整体策略是跟随课程提供的教程视频逐步操作，从最基础的工程配置开始，一步步建立对两套工具界面、操作逻辑与协作方式的理解。

### 1. 模块实现要点简述

- **空间音频（Room and Portal）**：在 Wwise 中为各个房间创建 Auxiliary Bus，挂载 RoomVerb 效果器；在 Unreal 中使用 AkSpatialAudioVolume 划定房间范围，通过 AkAcousticPortal 连接房间开口。

- **基础环境声与物件 3D Object**：通过 Attenuation 设置控制声音的空间化属性，环境声为 2D 背景音，物件声音为 3D 定位音。

- **角色脚步**：在角色的动画序列中添加 Footsteps Anim Notify，配合 Physical Material 和 Wwise 的 Switch Container，实现不同材质表面的脚步声切换。

---

## 三、上手困难与解决过程（重点）

### 困难一：Wwise 界面的"无从下手"

初次打开 Wwise 时，面对 Project Explorer、Transport Control、Property Editor、SoundBank Manager 等多个面板，完全不知道从哪个窗口开始操作。视频教程中讲师的操作速度较快，鼠标在不同面板间切换，难以跟上节奏。

**具体表现：**

- 不知道在哪个面板创建 Sound（右键菜单里选项太多，分不清 Audio Object、Sound SFX、Sound Voice 的区别）；
- 找不到 Attenuation 的编辑入口，教程里直接在 Property Editor 中展开，而我打开的窗口默认只显示基础属性；
- 对"总线"（Bus）与"辅助总线"（Auxiliary Bus）的概念混淆，不清楚应该在哪个层级挂载 RoomVerb 效果器。

**解决思路：** 首先放下教程，花了两天时间系统性地翻阅了 Wwise 官方文档的"工程结构"章节，理清了 Wwise 的顶层逻辑——工程（Project）→ 工作单元（Work Unit）→ 对象（Object），以及各类对象的从属关系（Bus 下挂 Child Bus，Sound 必须挂载到 Bus 或 Master Bus 下才能发声）。针对面板找不到的问题，发现 Wwise 的 Layout 切换（Design、Soundcaster、Profiler 等）会改变面板组合，教程中使用的 Layout 是默认的 Design，而我误触了其他 Layout 导致界面布局变化，切换回 Design 后所有面板恢复到了视频中的位置。

---

### 困难二：Wwise 与 Unreal 之间的概念映射关系不清晰

视频中提到了"在 Wwise 中创建 Event，在 Unreal 里用蓝图调用"，但对我这样的新手来说，Event、SoundBank、Game Parameter、Switch 等概念在 Wwise 中定义后，如何在 Unreal 中被识别和调用，完全是模糊的。

**具体表现：**

- 不知道 Wwise 工程修改后必须"生成 SoundBank"才能在 Unreal 中生效，经常在 Wwise 里改完声音后直接回 Unreal 测试，发现没有变化，反复重启软件仍然无效；
- 不清楚 Unreal 中的 AkComponent 与 Wwise 中 Event 的对应关系，在蓝图中拖动节点时看到一大堆 Ak 相关函数，不知道该用 Post Event 还是 Post Trigger；
- 对"Switch"和"State"的使用场景不清晰，在实现角色脚步切换时，误用了 State 而非 Switch，导致脚步声音始终不切换。

**解决思路：** 重新梳理工作流程，用纸笔绘制了一张概念映射表：

| Wwise 侧概念 | Unreal 侧对应 | 用途 |
| :--- | :--- | :--- |
| Event | AkComponent / Post Event 节点 | 播放某个声音 |
| SoundBank | 需生成 .bnk 并放入 Content/WwiseAudio | 打包所有声音资源 |
| Switch | Set Switch 蓝图节点 | 切换同一 Event 下的不同声音变体 |
| Auxiliary Bus | AkSpatialAudioVolume 中的 Aux Send | 空间混响效果 |

有了这张对照表后，后续操作中的"哪个概念对应哪个操作"就清晰了很多。针对 SoundBank 生成问题，将"修改 Wwise → 生成 SoundBank → 等待 Unreal 自动同步"这个三步流程写在了便签上贴在屏幕边缘，提醒自己不跳步。

---

### 困难三：Unreal 中 Wwise 相关组件的操作陌生

Unreal 原本的界面已经较为复杂，集成 Wwise 插件后又新增了大量 Ak 前缀的 Actor 组件和蓝图函数，在场景中放置声音、配置参数时经常搞混。

**具体表现：**

- 在场景中放置声音时，分不清应该用 Ambient Sound（UE原生）还是 AkComponent（Wwise 集成），导致放置后声音无法通过 Wwise 控制；
- 为 Room 创建 AkSpatialAudioVolume 时，不知道该勾选哪些参数，教程中直接跳过了一些默认选项的说明；
- 在角色蓝图（Character Blueprint）中添加 AkComponent 时，不清楚应该作为 Component 直接添加还是在 Event Graph 中动态生成。

**解决思路：** 针对场景放置的问题，将所有声音统一使用 AkComponent 和 AkAmbientSound 而非 UE 原生音频组件，保持全流程使用 Wwise 体系。针对参数不确定的情况，在测试场景中每次只调整一个参数并预览效果，通过对比逐步理解每个参数的含义（例如 Room 中的 Transmission Loss 数值越大，声音穿过墙体时衰减越明显）。针对角色脚步，参考了 UE 官方文档中"在 Animation Blueprint 中添加 Notify"的通用做法，将 Wwise 的 Post Event 节点挂载在 Notify 下方，而非在角色蓝图中额外添加 Component。

---

### 困难四：两个软件的工程路径与版本协同问题

**具体表现：**

- 首次配置 Wwise 工程时，不知道 Unreal 项目文件夹内的 WwiseProject 目录就是 Wwise 工程的存放位置，误以为需要分别管理两个独立的工程文件，导致 Unreal 中的 Wwise 插件找不到对应的 Wwise 工程；
- 教程视频使用的是 Wwise 2024 版，而我安装的是 2025.1 版，部分界面布局和操作路径不一致，导致按照教程截图寻找功能时花费大量时间。

**解决思路：** 通过 Audiokinetic Launcher 创建工程时，直接指定 Unreal 项目根目录下的 WwiseProject 文件夹，确保两个软件共享同一个 Wwise 工程源文件。版本差异方面，在 Wwise 官网文档页面右上角可以下拉切换 2024.1 / 2025.1 版本，遇到界面不一致时直接查阅 2025.1 版本的官方文档，同时利用 Wwise 内置搜索功能（快捷键 Ctrl+F）快速定位菜单或选项，避免了在差异面板上"凭运气找按钮"的低效操作。

---

### 困难五：GitHub上传过程中遇到的各类报错

完成项目开发后，将整个工程推送到GitHub仓库时遇到了多种阻碍，这也是新手在实际交付环节容易卡住的地方。

#### 具体表现一：大文件超过100MB限制

UE + Wwise 项目体积较大，Wwise 生成的 SoundBank 文件、音频资源文件以及 UE 的资产文件往往超过 GitHub 单个文件 100MB 的限制。执行 git push 时报错类似：

```
remote: error: File xxx.bnk is 135.17 MB; this exceeds GitHub's file size limit of 100.00 MB
remote: error: GH001: Large files detected. You may want to try Git Large File Storage
```

**原因分析：** GitHub 对普通文件有 100MB 的大小上限，超出即拒绝推送。音频项目中，SoundBank 文件、未压缩的 WAV 资源、UE 的 .uasset 缓存文件等都可能触及此限制。

**解决方法：** 使用 Git LFS（Large File Storage）管理大文件。Git LFS 用文本指针替换大文件，将文件内容存储在远程服务器上。操作步骤如下：

1. 安装 Git LFS 后执行 `git lfs install`（只需执行一次）
2. 在项目目录中运行 `git lfs track "*.bnk"` 和 `git lfs track "*.wav"` 追踪大文件类型
3. 先提交 `.gitattributes` 文件：`git add .gitattributes && git commit -m "add lfs config"`
4. 再提交大文件：`git add . && git commit -m "submit project" && git push`

需要注意的是，GitHub 免费用户 Git LFS 总存储空间约 1GB，超出需付费或被冻结账号，因此推送前应检查仓库体积。

#### 具体表现二：推送时报 403 错误或连接超时

在推送过程中还遇到了 403 Forbidden 和代理连接超时问题。搜索后发现国内网络环境下推送至 GitHub 时常因出口 IP 被风控封禁导致 403 报错。

**解决方法：** 使用 SSH 协议替代 HTTPS 协议推送，SSH 走 22 端口并配合密钥配对，可绕过 IP 检测的限制。具体步骤：

1. 生成 SSH 密钥：`ssh-keygen -t ed25519 -C "your_email@example.com"`
2. 复制公钥内容，在 GitHub Settings → SSH and GPG keys 中添加新密钥
3. 测试连接：`ssh -T git@github.com` 确认认证成功
4. 将本地仓库远程地址切换为 SSH 格式：`git remote set-url origin git@github.com:用户名/仓库名.git`
5. 重新推送即可

切换 SSH 后不再需要配置代理，推送也更稳定。

#### 具体表现三：误传大文件后如何清理

如果已经不小心提交了大文件到本地仓库但尚未推送成功，使用 `git reset` 回退提交记录后重新操作即可。对于已经推送上岸的大文件，需使用 `git filter-repo` 或 `git filter-branch` 从历史记录中彻底移除，避免 LFS 配额被浪费。

---

## 四、总结

本次实践最大的收获并非完成了四个模块的技术实现，而是在完全不熟悉两款专业工具的前提下，建立了一套从零开始的学习与排查方法。核心体会是：

1. **先建立概念框架再动手**：在动操作之前，花时间理解 Wwise 的"对象层级结构"和"Event-SoundBank-播放"这条主线逻辑，后续操作才不会迷失在面板里；
2. **制作自己的对照笔记**：将 Wwise 和 Unreal 之间的概念映射关系以表格形式记录下来，减少反复查阅视频的时间成本；
3. **版本一致性优先**：开始项目前确认 Wwise 设计工具版本与 Unreal Integration 版本严格一致，避免因版本不兼容导致难以排查的静音问题；
4. **善用官方文档而非依赖视频截图**：视频教程操作较快，遇到版本差异时，直接查阅对应版本的官方文档反而效率更高。

老师的视频教程很详细，虽然也遇到了一些问题，但总体而言通过这次实践，我对 UE + Wwise 的基本工作流有了整体认知，为后续更复杂的音频设计任务奠定了基础。
